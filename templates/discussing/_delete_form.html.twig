<form method="post" action="{{ path('app_discussing_delete', {'id': discussing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ discussing.id) }}">
    <button class="btn">Delete</button>
</form>
